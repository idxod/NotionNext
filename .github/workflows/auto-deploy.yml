name: Auto Deploy NotionNext

on:
  schedule:
    - cron: "*/10 * * * *"   # 每10分钟执行
  workflow_dispatch:         # 支持手动触发

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger build
        run: echo "Trigger build..."
